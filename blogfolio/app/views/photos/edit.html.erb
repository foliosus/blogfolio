<h1>Editing photo</h1>

<%= error_messages_for :photo %>

<% form_for(@photo) do |f| %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @photo %> |
<%= link_to 'Back', photos_path %>
