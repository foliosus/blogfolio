<h1>New photo</h1>

<%= error_messages_for :photo %>

<% uni_form_for :photo, @photo, :url => photos_path, :html => {:multipart => true} do |f| %>
  <p>
    <%= f.file_field :image %>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', photos_path %>
