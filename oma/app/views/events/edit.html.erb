<% uni_form_for(@event) do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial => "form", :locals => {:f => f} %>

  <p><%= f.submit "Update" %></p>
<% end %>

<p><%= link_to 'Show', @event %> | <%= link_to 'Back', events_path %></p>